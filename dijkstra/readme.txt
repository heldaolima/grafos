INSTRUÇÕES

O executável é o binário "dijkstra". Os comandos do prompt dados na lista são aceitos.

O programa espera que você insira um arquivo com o grafo no seguinte modelo:

{numero de vértices} {numero de arestas}
{vetice a} {vertice b} {peso da aresta}

O arquivo "entrada.dat" contém uma entrada de exemplo.